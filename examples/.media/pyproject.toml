[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aipi-531-reinforcement-learning"
version = "0.1.0"
description = "Video stitching utilities for course media"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "moviepy>=2.2.1",
]
